# Baseful Environment Variables

# The public IP or Tailscale IP of this machine
# Used for generating connection strings
PUBLIC_IP=localhost

# JWT Configuration - MUST BE AT LEAST 32 CHARACTERS
JWT_SECRET=replace-me-with-a-secure-random-string-at-least-32-chars

# Supavisor/Proxy Configuration
PROXY_PORT=6432
PROXY_HOST=0.0.0.0

Docker Network Configuration

# ============================================
# Security Configuration
# ============================================

# SSL/TLS Configuration for Database Proxy
# Enable SSL/TLS termination on the proxy (true/false)
PROXY_SSL_ENABLED=false

# Path to TLS certificate file (absolute path inside container)
PROXY_TLS_CERT_FILE=

# Path to TLS private key file (absolute path inside container)
PROXY_TLS_KEY_FILE=

# Path to Client CA file for mutual TLS authentication (optional)
PROXY_TLS_CA_FILE=

# Idle Connection Timeout
# Maximum time a connection can be idle before being closed
PROXY_IDLE_TIMEOUT=30m

# Query Timeout
# Maximum time a single query can execute before being cancelled
PROXY_QUERY_TIMEOUT=5m

# Enable Token Revocation Checking
PROXY_REVOCATION_CHECK=true

# Logging Configuration
PROXY_LOG_PATH=

# Maximum number of log entries to keep in memory
PROXY_MAX_LOG_ENTRIES=10000

# Docker Network Configuration
DOCKER_NETWORK=baseful-network

# Application Configuration
APP_PORT=8080
GIN_MODE=release
